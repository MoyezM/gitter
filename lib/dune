(include_subdirs qualified)

(library
 (name gitter)
 (libraries async bonsai bonsai_term sqlite3 bonsai_term_components.border_box
   core grammar_registry
   tree-sitter tree-sitter.json tree-sitter.ocaml)
 (foreign_stubs
  (language c)
  (names pty_stubs vterm_stubs)
  (flags
   (:standard
    (:include vterm_c_flags.sexp))))
 (c_library_flags
  (:include vterm_c_library_flags.sexp))
 (preprocess
  (pps ppx_jane bonsai.ppx_bonsai)))

(rule
 (targets vterm_c_flags.sexp vterm_c_library_flags.sexp)
 (action
  (run %{exe:../config/discover.exe})))
