# The nix dev shell provides every system dependency (see flake.nix); the
# `path:.` form works even while flake.nix is not yet committed. The opam
# switch is activated by the shell's own hook.
use flake path:.
