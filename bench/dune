(executable
 (name bench_diff)
 (libraries async core core_unix.command_unix gitter gitter_grammar_c tree-sitter)
 (link_flags (:include link_flags.sexp))
 (preprocess
  (pps ppx_jane)))

; Same flags as the shipped binary; see bin/dune, and test/dune for why the
; static archives are not optional on Linux.
(rule
 (copy ../bin/link_flags.sexp link_flags.sexp))
