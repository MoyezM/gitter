(include_subdirs qualified)

; terminal/ and term_pane/ are the ARCHIVED embedded-terminal component
; (libvterm + forkpty; see terminal/README.md) — kept out of the build so
; gitter no longer needs libvterm. Re-enable by removing them from this
; exclusion and restoring the stanzas listed in the README.
(dirs :standard \ terminal term_pane)

(library
 (name gitter)
 (libraries async bonsai bonsai_term bonsai_term_components.border_box
   core grammar_registry
   tree-sitter tree-sitter.json tree-sitter.ocaml)
 (preprocess
  (pps ppx_jane bonsai.ppx_bonsai)))
